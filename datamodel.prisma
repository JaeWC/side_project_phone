type User {
  id: ID! @unique
  email: String! @unique
  password: String!
  isVerified: Boolean!
  numbers: [Number!]!
  verificationSecret: String!
}

type Number {
  id: ID! @unique
  twilioId: String!
  number: String!
  owner: User!
}
